// import { Middleware } from '@nuxt/types'

// const guestMiddleware: Middleware = async function (context) {
//   const { redirect, $auth } = context

//   console.log($auth.isLoggedIn)
//   if ($auth.isLoggedIn) redirect('/dashboard')
// }

// export default Object.freeze(guestMiddleware)
